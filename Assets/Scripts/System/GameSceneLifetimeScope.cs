using VContainer;
using VContainer.Unity;

namespace Alija.Big2.Client
{
    public class GameSceneLifetimeScope : LifetimeScope
    {
        protected override void Configure(IContainerBuilder builder)
        {
        }
    }
}
